import { t } from '@/utils/language';

export const CMDB_RESOURCE_MAP = {
  compute: t('compute'),
  networking: t('networking'),
  storage: t('storage'),
  database: t('database'),
  domain: t('domain'),
  others: t('others'),
}

export const OPEN_C3_CLOUD_VENDOR = ['aws', 'qcloud', 'aliyun','ksyun','google','huawei', 'ibm']